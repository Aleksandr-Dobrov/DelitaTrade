To start the project, a connection string with name "DelitaConnection" in "ConnectionStrings" for SQL Server is required.

For initial user seeding is required to have configuration section "DelitaUsers" whit objects "AdminUser", "LogisticUser", "DriverUser"
each of them has keys: "UserName" ,"Password" ,"FirstName" ,"LastName" whit string values.
